#include<stdio.h>
#include"mystring.h"
void frequency(char*);
main()
{
	char s[100];
	printf("Enter A String : ");
	gets(s);
	mystrtolower(s);
	frequency(s);
}
void frequency(char *s)
{
	char c[50];
	while(*s!='\0')
	{
		do
		{
				
		}while(c!='0');
		
		if(key==*s)
		
		s++;
	}
	return(i);
}
